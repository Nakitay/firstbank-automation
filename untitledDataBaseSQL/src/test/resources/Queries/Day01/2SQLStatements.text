----------------- SQL Statements-------------------
1) SELECT : Specify a list of columns in the table from which we want to QUERY data in the SELECT  statement
-----COMMA  : We use comma between each column in case we want to query data from multiple columns
3) ALL COLUMN :  If we want to query data from all column we use asterisk(*) as the shorthand  for all columns
3) From : We indicate the table name after the FROM keyword SQL language is case INSENSITIVE

---------------Select DISTINCT STATEMENT-----------------
The  DISTINCT( removes duplicate)  keyword can be used to return only distinct =( different) values

----------------Where Statement-------------------------
The WHERE appears right after the FROM clause
The condition are to filter the rows returned from the select Statement
SELECT FROM WHERE

---------ORDER BY STATEMENT -----------------
allows us to sort the rows returned from select statement in ascending or Descending order based on criteria specified

-------------------Like Statement  ------------------------
Partial research. Pattern Matching
Select something LIKE or NOT LIKE this! You dont know full information, but you just know name .
That situation we use LIKE

--------COUNT statement -------------------
returns the number of input rows that match a specific condition of a query

--------------Group BY Statement ------------------
 Divides the rows returned from SELECT statement into groups

For each group, you can apply an aggregate function, for example:
Calculating the sum of them
count the number of items in the groups


-----------HAVING statement --------------------

--------------View-------------------












